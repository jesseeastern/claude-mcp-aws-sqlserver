# claude-mcp-aws-sqlserver
MCP Server that connects Claude AI to SQL Server running on a Windows EC2 instance inside an AWS VPC — enabling natural-language database queries, EC2 management, and remote command execution via SSM.
